function aiSetColor(r, g ,b, isStroke){    if ( app.documents.length > 0 ) {        var newRGBColor = new RGBColor();        newRGBColor.red = r;        newRGBColor.green = g;        newRGBColor.blue = b;        if (isStroke) {            app.activeDocument.defaultStrokeColor = newRGBColor;        } else {            app.activeDocument.defaultFillColor = newRGBColor;        }    } else {        return false;    }}function aiGetColor(isStroke){    if ( app.documents.length > 0 ) {        var result = new Array();        var color = app.activeDocument.defaultStrokeColor;        if (isStroke) {            color = app.activeDocument.defaultStrokeColor;        } else {            color = app.activeDocument.defaultFillColor;        }        result.r = color.red        result.g = color.green        result.b = color.blue                return result;    } else {        return false;    }}